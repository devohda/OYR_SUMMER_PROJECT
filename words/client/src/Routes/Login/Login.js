import React from 'react';
import AuthTemplate from '../Register/AuthTemplate';
import LoginForm from './LoginForm';

const Login = () => {
    return (
        <AuthTemplate>
            <LoginForm />
        </AuthTemplate>
    );
};

export default Login;
